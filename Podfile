# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReduxSwiftBoilerplate' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReduxSwiftBoilerplate

  pod 'ReSwift', '~> 4.0'
  pod 'SwiftCommonUtils', '~> 0.1'

  target 'ReduxSwiftBoilerplateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ReduxSwiftBoilerplateUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
